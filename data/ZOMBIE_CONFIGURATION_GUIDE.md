# 僵尸配置指南

## 📋 目录
1. [修改僵尸刷新顺序和时机](#修改僵尸刷新顺序和时机)
2. [修改僵尸属性参数](#修改僵尸属性参数)
3. [僵尸ID列表](#僵尸id列表)

---

## 修改僵尸刷新顺序和时机

### 📁 配置文件位置
**关卡配置文件：** `data/level_test.json` 和 `Resources/data/level_test.json`

### 📝 配置格式
在 `waves` 数组中，每个对象代表一个僵尸刷新事件：

```json
{
  "waves": [
    {
      "time": 2.0,        // 刷新时间（秒）- 游戏开始后多少秒刷新
      "zombieId": 2001,   // 僵尸ID（见下方ID列表）
      "row": 2            // 刷新行数（0-4，共5行）
    }
  ]
}
```

### 🔧 参数说明

| 参数 | 类型 | 说明 | 示例 |
|------|------|------|------|
| `time` | float | 游戏开始后多少秒刷新该僵尸 | `2.0` 表示游戏开始2秒后刷新 |
| `zombieId` | int | 僵尸类型ID | `2001`=普通僵尸, `2002`=路障僵尸, `2003`=铁桶僵尸 |
| `row` | int | 刷新在第几行 | `0-4`，共5行（从下往上） |

### 💡 示例

```json
{
  "waves": [
    {
      "time": 2.0,
      "zombieId": 2001,
      "row": 2
    },
    {
      "time": 5.0,
      "zombieId": 2002,  // 5秒后刷新路障僵尸
      "row": 0
    },
    {
      "time": 8.0,
      "zombieId": 2003,  // 8秒后刷新铁桶僵尸
      "row": 4
    },
    {
      "time": 10.0,
      "zombieId": 2001,
      "row": 1
    },
    {
      "time": 10.0,      // 同一时间可以刷新多个僵尸
      "zombieId": 2001,
      "row": 3
    }
  ]
}
```

### ⚠️ 注意事项
- `time` 值按时间顺序排列，但可以相同（同时刷新多个僵尸）
- `row` 范围是 0-4，超出范围可能导致错误
- 修改后需要重启游戏才能生效

---

## 修改僵尸属性参数

### 📁 配置文件位置
**僵尸数据配置文件：** `data/zombies.json` 和 `Resources/data/zombies.json`

### 📝 配置格式
每个僵尸类型使用其ID作为键：

```json
{
  "2001": {
    "name": "NormalZombie",
    "hp": 300,              // 生命值
    "speed": 10,            // 移动速度
    "damage": 20,           // 攻击伤害
    "attackInterval": 1.5,  // 攻击间隔（秒）
    "texture": "zombies/normalzombie/normalzombie_walk/1.png",
    "animations": { ... }
  }
}
```

### 🔧 参数说明

| 参数 | 类型 | 说明 | 默认值示例 |
|------|------|------|-----------|
| `name` | string | 僵尸名称（内部标识） | `"NormalZombie"` |
| `hp` | int | 生命值 | `300` |
| `speed` | int | 移动速度（像素/秒） | `10` |
| `damage` | int | 每次攻击造成的伤害 | `20` |
| `attackInterval` | float | 攻击间隔时间（秒） | `1.5` |
| `texture` | string | 默认纹理路径 | `"zombies/normalzombie/normalzombie_walk/1.png"` |
| `animations` | object | 动画配置（见下方） | - |
| `defaultAnimation` | string | 默认播放的动画 | `"walk"` |

### 💡 修改示例

**增强普通僵尸：**
```json
{
  "2001": {
    "name": "NormalZombie",
    "hp": 500,              // 从300增加到500
    "speed": 15,            // 从10增加到15（更快）
    "damage": 30,           // 从20增加到30
    "attackInterval": 1.0,  // 从1.5减少到1.0（攻击更快）
    ...
  }
}
```

**削弱路障僵尸：**
```json
{
  "2002": {
    "name": "ConeheadZombie",
    "hp": 400,              // 从640减少到400
    "speed": 8,             // 从10减少到8（更慢）
    ...
  }
}
```

### ⚠️ 注意事项
- 修改 `hp`、`speed`、`damage`、`attackInterval` 后需要重启游戏
- 不要修改 `name`、`texture`、`animations` 等资源相关字段，除非你同时更新了资源文件
- 建议先备份原配置文件

---

## 僵尸ID列表

| ID | 名称 | 默认HP | 说明 |
|----|------|--------|------|
| `2001` | NormalZombie | 300 | 普通僵尸 |
| `2002` | ConeheadZombie | 640 | 路障僵尸（戴路障） |
| `2003` | IronheadZombie | 1370 | 铁桶僵尸（戴铁桶） |

### 📝 在关卡配置中使用

在 `level_test.json` 的 `waves` 数组中，使用 `zombieId` 字段指定要刷新的僵尸类型：

```json
{
  "time": 5.0,
  "zombieId": 2002,  // 刷新路障僵尸
  "row": 2
}
```

---

## 🎮 快速参考

### 修改刷新顺序
1. 打开 `data/level_test.json`
2. 修改 `waves` 数组中的 `time` 和 `zombieId`
3. 保存并重启游戏

### 修改僵尸属性
1. 打开 `data/zombies.json`
2. 找到对应僵尸ID（如 `"2001"`）
3. 修改 `hp`、`speed`、`damage`、`attackInterval`
4. 保存并重启游戏

### 添加新僵尸类型
1. 在 `zombies/` 目录下创建新僵尸文件夹
2. 在 `data/zombies.json` 中添加新条目（使用新ID，如 `"2004"`）
3. 在 `data/animations.json` 中添加动画配置
4. 在关卡配置中使用新ID刷新僵尸

---

## 📚 相关文件

- **关卡配置：** `data/level_test.json`, `Resources/data/level_test.json`
- **僵尸数据：** `data/zombies.json`, `Resources/data/zombies.json`
- **动画配置：** `data/animations.json`
- **代码实现：** `Classes/Managers/LevelManager.cpp`, `Classes/Managers/DataManager.cpp`

